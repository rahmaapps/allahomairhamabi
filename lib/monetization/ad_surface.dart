/// Emplacements/écrans potentiellement concernés par une décision
/// publicitaire future (LOT 5.B+). Pur — aucune dépendance Flutter/widget,
/// pour rester testable en isolation (voir `ads_policy.dart`).
enum AdSurface {
  home,
  search,
  favorites,
  graveVisit,
  duaRead,
  onboarding,
  splash,
  shareAsImage,
}
